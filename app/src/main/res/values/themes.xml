<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ExerciseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/overlay_background</item>
        <item name="colorPrimaryVariant">@color/window_background</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_interact_colour</item>
        <item name="colorSecondaryVariant">@color/interaction_background_dim</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/status_background</item>

        <!-- WindowBackground -->
        <item name="android:windowBackground">@color/window_background</item>

        <!-- Text style -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">12dp</item>
    </style>

    <!-- Style Bottom Nav Bar -->
    <style name="ExerciseBottomNavStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/bottom_nav_color_state_list</item>
        <item name="itemTextColor">@color/bottom_nav_color_state_list</item>
        <item name="itemBackground">@color/overlay_background</item>
        <item name="elevation">6dp</item>
    </style>
    <!-- Style Buttons -->
    <style name="ExerciseButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/secondary_interact_colour</item>
        <item name="backgroundTint">@color/primary_interact_colour</item>
        <item name="colorOnSurface">@color/interact_disabled_colour</item>
    </style>
    <style name="ShapeAppearanceOverlay.WorkoutApp.Button.Circle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="ExerciseAddButton">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/add_button</item>
        <item name="colorOnSurface">@color/add_button_disabled</item>
    </style>

    <!-- Style TabLayout Bar -->
    <style name="ExerciseTabLayoutStyle" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabIndicatorColor">@color/primary_interact_colour</item>
        <item name="tabTextColor">@color/tab_color_state_list</item>
        <item name="tabSelectedTextColor">@color/primary_interact_colour</item>
        <item name="tabRippleColor">@color/primary_interact_colour</item>
        <item name="android:background">@color/window_background</item>
    </style>
    <!--  Style toolbar  -->
    <style name="ToolbarTitleWhite" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <!--  Text Title  -->
    <style name="ExerciseTextTitleStyle">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>
    <!--  Text body  -->
    <style name="ExerciseTextBodyStyle">
        <item name="android:textSize">18sp</item>
    </style>

</resources>